#!/usr/bin/env bash
set -euo pipefail

brightnessctl -d 'smc::kbd_backlight' set +10% >/dev/null
value="$(brightnessctl -d 'smc::kbd_backlight' -m | cut -d, -f4 | tr -d '%')"

osd "Keyboard" "$value" "⌨"
